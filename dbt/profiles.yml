tenant_alert:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('GCP_PROJECT_ID') }}"
      dataset: bronze
      threads: 4
      timeout_seconds: 300
      location: US
      priority: interactive
